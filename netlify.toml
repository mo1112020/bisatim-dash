[build]
  command = "npm run build"

[build.environment]
  NODE_VERSION = "20"

[[plugins]]
  package = "@netlify/plugin-nextjs"

# Server Actions + CDN uploads (up to 50mb in next.config)
[functions]
  node_bundler = "esbuild"
